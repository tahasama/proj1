pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'pip install - requirements.tx'
            }
        }

        stage('Test') {
            steps {
                echo 'python manage.py test'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo not yet...taha'
            }
        }
    }
}