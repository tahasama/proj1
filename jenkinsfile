pipeline {
    agent {
        // docker { image 'python:3.9' }
        docker { image 'docker' }
    }
    // environment {
    //     PATH = "$PATH:/usr/local/bin"

    // }
    stages {
        // stage('Build') {
        //     steps {
        //         sh 'pip install -r requirements.txt'
        //     }
        // }

        stage('Test') {
            steps {
              sh 'docker -v'  // sh '/usr/local/bin/docker-compose run web python manage.py test'
              echo 'df df dfdfdf'  
               
              sh 'docker-compose -v'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo not yet...taha'
            }
        }
    }
}